services:
  - type: web
    name: deckfront
    runtime: docker
    branch: main
    plan: "0.5c-512mb"
    numInstances: 1
    dockerContext: .
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    disk:
      name: deckfront-data
      mountPath: /var/data
      sizeGB: 1
