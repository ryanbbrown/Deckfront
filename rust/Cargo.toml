[workspace]
members = ["goldfish"]
resolver = "2"

[workspace.package]
edition = "2024"
rust-version = "1.98"
license = "MIT"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
